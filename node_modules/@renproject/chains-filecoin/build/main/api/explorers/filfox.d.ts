import { FilNetwork, FilTransaction } from "../../deposit";
export declare class Filfox {
    constructor(network?: FilNetwork);
    fetchDeposits: (address: string, paramsFilterBase64?: string | undefined, page?: number, size?: number) => Promise<{
        deposits: FilTransaction[];
        totalCount: number;
    }>;
    fetchMessage: (cid: string) => Promise<FilTransaction>;
}
