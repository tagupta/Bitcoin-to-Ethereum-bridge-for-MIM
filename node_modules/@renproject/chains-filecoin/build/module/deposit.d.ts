export declare type FilNetwork = "mainnet" | "testnet" | "devnet";
export interface FilTransaction {
    cid: string;
    amount: string;
    params: string;
    confirmations: number;
    nonce: number;
    reverted?: boolean;
}
export interface FilExplorer {
    fetchDeposits: (address: string, params?: string | undefined, page?: number) => Promise<FilTransaction[]>;
}
