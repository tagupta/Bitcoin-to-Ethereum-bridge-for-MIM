import { Logger } from "@renproject/interfaces";
import { HttpProvider } from "./httpProvider";
import { Provider } from "./jsonRPC";
export declare class DarknodeProvider<Requests extends {
    [event: string]: any;
} = {}, Responses extends {
    [event: string]: any;
} = {}> extends HttpProvider<Requests, Responses> implements Provider<Requests, Responses> {
    constructor(multiAddress: string, logger?: Logger);
}
