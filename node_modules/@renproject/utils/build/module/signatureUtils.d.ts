/// <reference types="node" />
import { Logger } from "@renproject/interfaces";
import BigNumber from "bignumber.js";
interface Signature {
    r: Buffer;
    s: Buffer;
    v: number;
}
export declare const signatureToBuffer: <T extends Signature>(sig: T) => Buffer;
export declare const secp256k1n: BigNumber;
export declare const fixSignatureSimple: (r: Buffer, s: Buffer, v: number) => Signature;
export declare const fixSignature: (r: Buffer, s: Buffer, v: number, sigHash: Buffer, pHash: Buffer, amount: string, to: string, tokenIdentifier: string, nHash: Buffer, v2?: boolean | undefined, logger?: Logger) => Signature;
export {};
