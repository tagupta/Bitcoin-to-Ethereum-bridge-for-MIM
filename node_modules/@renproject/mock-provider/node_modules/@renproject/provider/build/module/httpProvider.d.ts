import { Logger } from "@renproject/interfaces";
import { Provider } from "./jsonRPC";
export declare class HttpProvider<Requests extends {
    [event: string]: any;
} = {}, Responses extends {
    [event: string]: any;
} = {}> implements Provider<Requests, Responses> {
    readonly nodeURL: string;
    readonly logger: Logger;
    constructor(ipOrMultiaddress: string, logger?: Logger);
    sendMessage: <Method extends keyof Requests & string>(method: Method, request: Requests[Method], retry?: number, timeout?: number) => Promise<Responses[Method]>;
    private readonly responseError;
}
