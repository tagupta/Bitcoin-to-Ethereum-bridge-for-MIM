import { ContractCall, EthArg } from "@renproject/interfaces";
export declare enum OverrideContractCallError {
    OverrideArrayLengthError = "Contract call override must be same length as contract calls array."
}
export declare type ContractCallOverride = Partial<Omit<ContractCall, "contractParams"> & {
    contractParams: Array<Partial<EthArg>>;
}>;
export declare const overrideContractCalls: (contractCalls: ContractCall[], override: ContractCallOverride | ContractCallOverride[]) => ContractCall[];
