/**
 * The `value` function converts between different cryptocurrency units.
 * See `value.spec.ts` for example usage.
 */
import { NumberValue } from "@renproject/interfaces";
export declare const toSmallestUnit: (value: NumberValue, decimals: number) => string;
export declare const fromSmallestUnit: (value: NumberValue, decimals: number) => string;
